# santa-hat :tophat: :rabbit:

Randomizes participants for gift assignation

## Example

Edit `players` variable to match your needs, or let it empty to compose it
on the run.

Run the script compute a gifts list:

```bash
ruby ./santa_hat.rb
```
